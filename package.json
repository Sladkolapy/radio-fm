{
  "name": "genius-application-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix prototype run dev",
    "build": "npm --prefix prototype run build",
    "preview": "npm --prefix prototype run preview"
  }
}
